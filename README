Backports of layers included in Venus OS.

Don't backport anything newer than the next scheduled LTS here,
the intend is that this can simply start with a new empty branch
whenever switching to a new LTS.
